---
description: Install the Squirrel status line into your Claude Code settings
---

Run this exact command with the Bash tool and show the user its output verbatim:

`sh "${CLAUDE_PLUGIN_ROOT}/scripts/squirrel.sh" --setup "${CLAUDE_PLUGIN_ROOT}"`

Then tell the user, in at most three lines: the status line is active (it may take a moment to appear), the activity-state hooks are active because the plugin is installed, and they can label this account with `/squirrel-nick <name>`. Do not do anything else.
