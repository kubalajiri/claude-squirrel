---
description: Set the fallback colour for accounts with no colour of their own
argument-hint: <colour>
---

Run this exact command with the Bash tool and show the user its output verbatim:

`sh "${CLAUDE_PLUGIN_ROOT}/scripts/squirrel.sh" --set-default-color "$ARGUMENTS"`

Do not do anything else.
