---
description: Set how long Claude may sit idle before the status line raises a loud alarm
argument-hint: <seconds|off>
---

Run this exact command with the Bash tool and show the user its output verbatim:

`sh "${CLAUDE_PLUGIN_ROOT}/scripts/squirrel.sh" --set-alarm "$ARGUMENTS"`

Do not do anything else.
