---
description: Toggle whether the 5h segment folds in this session's contribution
argument-hint: <on|off>
---

Run this exact command with the Bash tool and show the user its output verbatim:

`sh "${CLAUDE_PLUGIN_ROOT}/scripts/squirrel.sh" --set-session-contribution "$ARGUMENTS"`

Do not do anything else.
