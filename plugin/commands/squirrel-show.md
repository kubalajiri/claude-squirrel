---
description: Show or hide a status line segment (git, cost, tokens, pr, …)
argument-hint: <segment> <on|off>
---

Run this exact command with the Bash tool and show the user its output verbatim:

`sh "${CLAUDE_PLUGIN_ROOT}/scripts/squirrel.sh" --show-segment "$ARGUMENTS"`

Do not do anything else.
