---
description: Explain what Squirrel shows and how to configure it
---

Run this exact command with the Bash tool:

`sh "${CLAUDE_PLUGIN_ROOT}/scripts/squirrel.sh" --help-text`

Then summarise for the user, in a short table, what the status line shows and the handful of
commands they are most likely to want (`/squirrel-nick`, `/squirrel-show`, `/squirrel-alarm`,
`/squirrel-bg`, `/squirrel-config`). Mention they can simply ask in plain language — for example
"hide the cost" or "warn me sooner when it's idle" — and Claude will apply it.
