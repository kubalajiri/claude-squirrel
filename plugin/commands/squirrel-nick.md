---
description: Set a short nickname for the account you are logged in as
argument-hint: <nickname>
---

Run this exact command with the Bash tool and show the user its output verbatim:

`sh "${CLAUDE_PLUGIN_ROOT}/scripts/squirrel.sh" --set-nick "$ARGUMENTS"`

Do not do anything else.
