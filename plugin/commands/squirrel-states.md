---
description: Choose which waiting states trigger the alarm or the background tint
argument-hint: <alarm|background> <states>
---

Run this exact command with the Bash tool and show the user its output verbatim:

`sh "${CLAUDE_PLUGIN_ROOT}/scripts/squirrel.sh" --set-states "$ARGUMENTS"`

Do not do anything else.
