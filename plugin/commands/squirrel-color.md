---
description: Set the status line colour for the account you are logged in as
argument-hint: <colour name or #rrggbb>
---

Run this exact command with the Bash tool and show the user its output verbatim:

`sh "${CLAUDE_PLUGIN_ROOT}/scripts/squirrel.sh" --set-color "$ARGUMENTS"`

Do not do anything else.
