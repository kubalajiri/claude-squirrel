---
description: Override a segment's display label (e.g. rename 'mine')
argument-hint: <segment> <text|reset>
---

Run this exact command with the Bash tool and show the user its output verbatim:

`sh "${CLAUDE_PLUGIN_ROOT}/scripts/squirrel.sh" --set-label "$ARGUMENTS"`

Do not do anything else.
