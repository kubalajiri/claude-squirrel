---
description: Choose which delta drives cross-session attribution (cost or tokens)
argument-hint: <cost|tokens>
---

Run this exact command with the Bash tool and show the user its output verbatim:

`sh "${CLAUDE_PLUGIN_ROOT}/scripts/squirrel.sh" --set-attribution-metric "$ARGUMENTS"`

Do not do anything else.
