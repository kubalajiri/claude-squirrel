---
description: Warn me as the 5-hour session reset approaches (so I can compact first)
argument-hint: <warn minutes> <urgent minutes> | off
---

Run this exact command with the Bash tool and show the user its output verbatim:

`sh "${CLAUDE_PLUGIN_ROOT}/scripts/squirrel.sh" --set-reset-warn "$ARGUMENTS"`

Do not do anything else.
