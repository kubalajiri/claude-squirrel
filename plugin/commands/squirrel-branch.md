---
description: Limit how many characters of the git branch name are shown (0 = no limit)
argument-hint: <max characters>
---

Run this exact command with the Bash tool and show the user its output verbatim:

`sh "${CLAUDE_PLUGIN_ROOT}/scripts/squirrel.sh" --set-branch-max "$ARGUMENTS"`

Do not do anything else.
