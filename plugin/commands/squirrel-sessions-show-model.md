---
description: Toggle the per-model mix column in the /squirrel-sessions table
argument-hint: <on|off>
---

Run this exact command with the Bash tool and show the user its output verbatim:

`sh "${CLAUDE_PLUGIN_ROOT}/scripts/squirrel.sh" --set-sessions-show-model "$ARGUMENTS"`

Do not do anything else.
